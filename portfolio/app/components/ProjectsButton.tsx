'use client';

import React from 'react';

const ProjectsButton = () => {
  return (
    <div className="projects-button">
      <button onClick={() => console.log("Click test")}>Projects</button>
    </div>
  );
}

export default ProjectsButton;